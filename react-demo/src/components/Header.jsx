const Header = ({title}) => {
    return(
      <nav className="navbar navbar-light bg-light">
        <a className="navbar-brand" href="#"><h1>{title}</h1></a>
      </nav>
    )
    // ) <;
  };

export default Header;